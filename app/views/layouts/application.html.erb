<!DOCTYPE html>
<html>
  <head>
    <title>RailsWiki</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>
  </head>

  <body style="background-color: #e8e6e6; height: 100%">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">RailsWIKI</a>
    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse" id="navbarColor01" style="">
      <ul class="navbar-nav mr-auto"></ul>
      <form>
        <% if user_signed_in? %>
          <li><%= link_to 'New Article', new_article_path, class: 'navbar-link', style: 'color: white' %></li>
          <li><%= link_to 'Sign out', destroy_user_session_path, style: 'color: white', :method => :delete %></li>
        <% else %>
          <li><%= link_to "Sign Up", new_user_registration_path , style: 'color: white'%></li>
          <li><%= link_to "Sign In", new_user_session_path , style: 'color: white'%></li>
        <% end %>
      </form>
    </div>
  </nav>

  <div class="container">
    <div class="row">
      <div class="col-lg-9" style="padding-top: 50px">
        <%= yield %>
      </div>
      <div class="col-lg-1"></div>
      <div class="col-lg-2; effect8 p-3 mb-5" style="background-color: #c7c7c7;
          min-height: 91vh; border-top-style: none">
        <ul class="nav nav-pills flex-column" style="margin-top: 15px; font-weight: bold">
          <li class="nav-item"><%= link_to 'All Categories', root_path %></li>
          <% Category.all.each do |category| %>
          <li class="nav-item" style="padding-top: 3px"><%= link_to category.name, articles_path(category: category.name) %></li>
          <% end %>
        </ul>
      </div>
    </div>

  </div>

  </body>
</html>
